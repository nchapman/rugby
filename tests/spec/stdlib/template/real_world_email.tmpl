{# data: {
  "user": {"name": "Alice Johnson", "email": "alice@example.com"},
  "order": {
    "id": "ORD-12345",
    "date": "2024-03-15",
    "items": [
      {"name": "Rugby Ball", "quantity": 2, "price": 29.99},
      {"name": "Jersey", "quantity": 1, "price": 59.99},
      {"name": "Shorts", "quantity": 3, "price": 24.99}
    ],
    "shipping": 9.99,
    "discount": 15.00
  },
  "company": {"name": "Rugby Gear Co.", "support_email": "support@rugbygear.com"}
} #}
Dear {{ user.name }},

Thank you for your order #{{ order.id }}!

Order Summary:
{% for item in order.items %}
  {{ forloop.index }}. {{ item.name }}
     Qty: {{ item.quantity }} × ${{ item.price }} = ${{ item.quantity | times: item.price }}
{% endfor %}
----------------------------------------
Subtotal: ${{ order.items | map: "price" | sum }}
Shipping: ${{ order.shipping }}
Discount: -${{ order.discount }}
----------------------------------------
TOTAL: ${{ order.items | map: "price" | sum | plus: order.shipping | minus: order.discount }}
{% if order.items.size > 2 %}
Wow, that's a great haul! You ordered {{ order.items.size }} different items.
{% endif %}
Questions? Contact us at {{ company.support_email }}

Best regards,
{{ company.name }}
